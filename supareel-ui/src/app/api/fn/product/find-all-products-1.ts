/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseProductByIdResponse } from '../../models/page-response-product-by-id-response';

export interface FindAllProducts1$Params {
  page?: number;
  size?: number;
}

export function findAllProducts1(http: HttpClient, rootUrl: string, params?: FindAllProducts1$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseProductByIdResponse>> {
  const rb = new RequestBuilder(rootUrl, findAllProducts1.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseProductByIdResponse>;
    })
  );
}

findAllProducts1.PATH = '/';
